import React, { Component } from "react";

class Scramble extends Component {
  render() {
    return (
      <div>
        <h4>{this.props.scramble}</h4>
      </div>
    );
  }
}

export default Scramble;
